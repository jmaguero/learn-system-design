# Reverse proxy vs load balancer

## Reverse proxy
Sits in front of servers; can terminate TLS, cache, compress, and route.

## Load balancer
Primarily spreads traffic across many backends; often overlaps with reverse proxies in practice.

## Practice prompts
- Draw a simple edge → proxy/LB → app → DB diagram.

## My notes
