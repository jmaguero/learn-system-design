# Where to cache (client → CDN → edge → app → DB)

## Layers
Client caching, CDN caching, web/proxy caching, application caching, database caching.

## Practice prompts
- For an API with auth, what must be in the cache key to avoid data leaks?

## My notes
