# HTTP, TCP, UDP

## TCP
Reliable, ordered delivery; higher overhead.

## UDP
Best-effort; lower latency; common in real-time media.

## HTTP
Application protocol commonly over TCP (or QUIC for HTTP/3).

## Practice prompts
- Why can tail latency spike even when average latency looks fine?

## My notes
